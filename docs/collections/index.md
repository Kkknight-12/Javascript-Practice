# Collections

Source content:

```text
src/collections/map/
```

Recommended publishing order:

1. JavaScript `Map` basics.
2. Iterating over `Map`.
3. When to use `Map` instead of plain objects.

Keep `Array.prototype.map` content in the arrays section.
